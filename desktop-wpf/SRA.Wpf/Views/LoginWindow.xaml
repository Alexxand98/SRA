<Window x:Class="SRA.Wpf.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login Administrador" Height="280" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Correo electrónico:" Grid.Row="0" Margin="0,0,0,5"/>
        <TextBox x:Name="EmailTextBox" Grid.Row="1" Height="30"/>

        <TextBlock Text="Contraseña:" Grid.Row="2" Margin="0,10,0,5"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Height="30"/>

        <Button Content="Iniciar sesión" Grid.Row="3" Height="35" Margin="0,66,0,-46" Width="120" HorizontalAlignment="Center"
                Click="Login_Click"/>
    </Grid>
</Window>
